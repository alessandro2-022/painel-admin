import React from 'react';

// A funcionalidade de login foi removida da aplicação a pedido do usuário.
// Este componente agora retorna null para evitar erros de renderização.
const Login: React.FC = () => {
    return null;
};

export default Login;
