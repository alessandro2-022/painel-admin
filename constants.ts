// Define a URL base para todas as chamadas de API.
// Mudar esta variável permite apontar para diferentes ambientes (desenvolvimento, produção, etc.).
export const API_BASE_URL = 'https://api.goly.com/v1'; // Exemplo de URL de produção

// Define a URL para o servidor WebSocket para comunicação em tempo real.
export const WEBSOCKET_URL = 'wss://ws.goly.com'; // Exemplo de URL de WebSocket
